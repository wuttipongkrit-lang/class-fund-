# 💰 ระบบจัดการเงินกองกลาง / เก็บเงินชั้นปี (Class Fund Tracker)

เว็บแอปพลิเคชันสำหรับบริหารจัดการเงินกองกลางประจำห้องเรียน/ชั้นปี รองรับการติดตามสถานะการชำระเงิน บันทึกบิลรายจ่าย คำนวณสรุปยอดเงินคงเหลืออัตโนมัติ สร้าง QR Code พร้อมเปย์ และสรุปข้อความสำหรับส่งเข้า LINE กลุ่ม

---

## 🔑 รหัสสำหรับเข้าใช้งาน (Default Login)

* **สิทธิ์เหรัญญิก (Admin Controller):** 
  * **รหัส:** `69000` (หรือ `1234`)
  * **ความสามารถ:** ตรวจรับยอดเงิน, เปลี่ยนสถานะชำระเงิน, เพิ่ม/ลบรายชื่อสมาชิก, บันทึกบิลรายจ่าย, ตั้งค่าเบอร์พร้อมเปย์
* **สิทธิ์นักศึกษา/สมาชิกห้อง:** 
  * **รหัส:** `69001` ถึง `69010` (ตรงตามเลขที่ในระบบ)
  * **ความสามารถ:** ดูสถานะยอดค้างชำระส่วนตัว, ดูสรุปรายรับ-รายจ่ายของห้อง, เปิด QR Code พร้อมเปย์สำหรับสแกนโอนเงิน

---

## 🚀 ขั้นตอนการนำขึ้นออนไลน์ฟรีด้วย GitHub Pages (Step-by-Step)

### ขั้นตอนที่ 1: เตรียมไฟล์ในเครื่อง
1. สร้างโฟลเดอร์ใหม่ในคอมพิวเตอร์ของคุณ ชื่อ `class-fund`
2. คัดลอกโค้ดจากไฟล์ `index.html` และ `README.md` นำไปไว้ในโฟลเดอร์นั้น

### ขั้นตอนที่ 2: สร้าง Repository บน GitHub
1. เข้าไปที่ [GitHub.com](https://github.com) และทำการเข้าสู่ระบบ (หากยังไม่มีบัญชี ให้กด Sign up ฟรี)
2. กดปุ่ม **"+"** ที่มุมขวาบน แล้วเลือก **New repository**
3. ตั้งชื่อ Repository เช่น `class-fund-2569`
4. เลือกสถานะเป็น **Public** (สาธารณะ)
5. กดปุ่ม **Create repository**

### ขั้นตอนที่ 3: อัปโหลดไฟล์ขึ้น GitHub
1. ในหน้า Repository ที่เพิ่งสร้าง ให้มองหาข้อความ *"uploading an existing file"* แล้วคลิกที่ลิงก์นั้น
2. ลากไฟล์ `index.html` และ `README.md` วางลงในช่องอัปโหลด
3. กดปุ่มสีเขียว **Commit changes** ด้านล่างสุด

### ขั้นตอนที่ 4: เปิดใช้งาน GitHub Pages (ให้กลายเป็นเว็บออนไลน์)
1. ไปที่แถบเมนู **Settings** ด้านบนของ Repository
2. ที่เมนูด้านซ้าย ให้เลือก **Pages** (ในหมวด Code and automation)
3. ในส่วน **Build and deployment > Branch**:
   * เปลี่ยนช่องจาก `None` ให้เลือกเป็น `main` (หรือ `master`)
   * ช่องโฟลเดอร์ข้างๆ เลือกเป็น `/ (root)`
4. กดปุ่ม **Save**
5. รอระบบประมวลผลประมาณ 1-2 นาที ให้ลองกดรีเฟรชหน้าเว็บ คุณจะเห็นแถบสีเขียวพร้อมลิงก์เว็บไซต์ เช่น:
   `https://<your-username>.github.io/class-fund-2569/`

---

## 📱 การใช้งานบนมือถือ
คุณสามารถคัดลอกลิงก์ GitHub Pages ส่งเข้า LINE กลุ่มของห้อง เพื่อนๆ และเหรัญญิกจะสามารถกดเปิดหน้าเว็บได้ทันทีบนสมาร์ตโฟนทุกรุ่น